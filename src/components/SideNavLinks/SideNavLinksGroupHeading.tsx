import styled from 'styled-components';

export const SideNavLinksGroupsHeading = styled.div`
    font-size: var(--font-size-1);
    color: var(--generalColor-60);
    margin: 2.8rem 0 1.2rem 0;
    padding-left: var(--spacing-5);
`;
