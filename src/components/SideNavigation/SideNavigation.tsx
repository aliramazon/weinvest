import styled from 'styled-components';

const SideNavigation = styled.div`
    background-color: var(--generalColor-90);
    width: 25rem;
    height: 100vh;
    padding: var(--spacing-5);
`;

export default SideNavigation;
